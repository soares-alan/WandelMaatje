spring:
  application:
    name: backend

  datasource:
    url: jdbc:h2:mem:wandelmaatje
    driver-class-name: org.h2.Driver
    username: sa
    password:

  sql:
    init:
      mode: always

  jpa:
    hibernate:
      ddl-auto: none

  h2:
    console:
      enabled: true
      path: /h2-console
